import drv;
import string;
fn main(): void {
    console.log("start");
    let cur = drv.find();
    let n = 0;
    while (n < 10) {
        let s = cur.next();
        console.log(`got: ${s}`);
        if (string.eql(s, "END")) { break; }
        n = n + 1;
    }
    console.log("done");
}
