// NEGATIVE test: must FAIL to compile.
// Calling a function with the wrong number of arguments is an error
// (A3 — arg-count checking for unambiguous top-level functions).
import assert;
fn add(a: int, b: int): int { return a + b; }
@test
fn t(): void {
    let x = add(1);
}
