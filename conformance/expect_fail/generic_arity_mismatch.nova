// NEGATIVE test: must FAIL to compile.
// Pair has two type parameters <A, B>; instantiating with one type argument is
// a compile error (A2 increment 2c — generic instantiation arity checking).
struct Pair<A, B> {
    pub first: A,
    pub second: B,
    init(a: A, b: B) {
        self.first = a;
        self.second = b;
    }
}

@test
fn t(): void {
    let p = Pair<int>(1, 2);
}
