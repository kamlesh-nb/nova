// EXPECT-FAIL: parse
// NEGATIVE test: `try`/`catch` were REMOVED with `throw` (specs §5.5, plan P2-12).
// See throw_removed.nova for the measured reasons. Reserved, not deleted, so this
// reports what happened instead of a confusing identifier error.
@test
fn t(): void {
    try {
        let x = 1;
    } catch (err) {
        let y = 2;
    }
}
