// HARNESS SELF-TEST fixture. This program COMPILES and RUNS cleanly (exit 0).
// The harness's classify_failure MUST label this "COMPILED-AND-RAN" — never a
// rejection kind. This is the OTHER way a negative gate silently lies: a case
// that SHOULD have been rejected but compiled+ran must not be mistaken for a
// rejection. run.sh asserts this and ABORTS if the guard mislabels it.

@test
fn t_valid(): void {
    let x = 1 + 1;
    console.log(x);
}
