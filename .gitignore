# Zig build
zig-out/
.zig-cache/
zig-pkg/
# kyte build output
build/
# vendored deps: keep source, ignore their build artifacts
deps/*/build/
deps/*/.zig-cache/
deps/*/zig-out/
deps/*/build_*/
# test/debug artifacts
__kyte_test
__kyte_test.ll
*.dSYM/
*.o
# local scratch
my_app/
recovered/
.DS_Store
*.log

# Local crypto reference copy (not part of the build)
deps/BearSSL/
__kyte_test.exe
__kyte_test.pdb
*.pdb

# Debug dump of the merged front-end source (KYTE_DUMP_MERGED=1); never part of the build
merged.ky

# Claude Code session worktrees/artifacts
.claude/
